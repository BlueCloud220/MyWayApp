package com.example.mywayapp.utils

/*import at.favre.lib.crypto.bcrypt.BCrypt

fun hashPassword(password: String): String {
    // El costo (12 en este caso) se puede ajustar según tus necesidades
    return BCrypt.withDefaults().hashToString(12, password.toCharArray())
}

fun verifyPassword(enteredPassword: String, storedHash: String): Boolean {
    val result = BCrypt.verifyer().verify(enteredPassword.toCharArray(), storedHash)
    return result.verified
}*/